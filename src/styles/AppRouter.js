import styled from 'styled-components';
import { Colors, Spacing } from '../constants';

export const BaseStyledDiv = styled.div`
    font-size: 62.5%;
    color: ${Colors.dark_grey};
    font-family: Helvetica, Arial, sans-serif;
    font-size: ${Spacing.m_size};
    line-height: 1.6;
`;